/* Sometime we need to store list of objects like list of colors user selected or list of shopping cart in situation like that we use an array to store that list */

/* An array is a data structer that we use to represent a list of items */
let selectedColors = ['red', 'blue'];
selectedColors[2]=1
// console.log(selectedColors);
// console.log(selectedColors);
console.log(selectedColors.length);