let x =1;

// Relational Oprator
// console.log(x>0);
// console.log(x>=1);
// console.log(x<1);
// console.log(x<=1);

// Equality
console.log(1 === 1); //Strict equality operator ensures that both these value we have must have the same type and same value

console.log('1' === 1);

console.log(1 == 1); //Lose equality operator only checks the value not the type
console.log('1' == 1); //we will get true 
console.log(true == 1); //the operator looks at the left side it is true so it will automatically convert the value on the right to boolean it will be like (true == true) 